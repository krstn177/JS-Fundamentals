function multiplication(parameter){
    for(let i = 1; i <= 10; i++){
        let sequence = parameter + ' ' + 'X' + ' ' + i + ' ' + '='+ ' '+ parameter*i;
        console.log(sequence);
    }   
}
